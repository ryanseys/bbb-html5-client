h1= title
p Please type a username and meeting ID below and click Sign in to start chatting.

script(src='/javascripts/index/index.js') //all index javascript

form(method='post', name='login')
  input(type="text", maxlength="#{max_u}", name="user[name]", placeholder='Enter a username')
  input(type="text", maxlength="#{max_mid}", name="meeting[id]", placeholder="Enter a meesting ID")
  input(type="submit", value='Sign in')

